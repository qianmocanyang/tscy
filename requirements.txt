# ============================================================
# 游戏实时同声传译 (tscy) —— 依赖清单
# 说明：核心链路只需前 5 项；其余为可选项，缺失时程序自动降级。
# ============================================================

# ---- 核心：音频采集 ----
numpy>=1.26,<3
sounddevice>=0.5.0

# ---- 核心：语音识别 (faster-whisper = CTranslate2 推理，无需 torch) ----
faster-whisper>=1.1.0

# ---- 核心：全局热键 (Windows 下需管理员权限运行) ----
keyboard>=0.13.5

# ---- 核心：翻译后端 HTTP 请求 ----
requests>=2.31.0

# ---- 可选：语音播报 (在线，质量高) ----
edge-tts>=6.1.9

# ---- 可选：语音播报 (离线兜底，Windows 依赖 pywin32) ----
pyttsx3>=2.90
pywin32>=306 ; sys_platform == "win32"

# ---- 可选：千问 (通义千问) 云端语音 ASR/TTS ----
dashscope>=1.20.0
